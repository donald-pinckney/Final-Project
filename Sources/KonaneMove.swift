class KonaneMove { //Class simply holds move values.

    init(fromX: Int, fromY: Int, toX: Int, toY: Int) {
    	self.fromX = fromX
    	self.fromY = fromY
    	self.toX = toX
    	self.toY = toY
    }
    let fromX: Int
    let fromY: Int
    let toX: Int
    let toY: Int

}